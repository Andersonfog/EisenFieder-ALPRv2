# EisenFieder Surveillance backend (FastAPI). Installs cleanly on a laptop
# (SQLite + local file storage by default).

fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
pydantic>=2.6
PyJWT>=2.8
python-multipart>=0.0.9        # required for multipart vehicle uploads
python-dotenv>=1.0             # optional .env loading

# Optional: real SMS alerts for watchlist hits. Without it, alerts are logged.
# twilio>=8.0

# Dev / tests / edge simulator
httpx>=0.27                    # FastAPI TestClient
requests>=2.31
